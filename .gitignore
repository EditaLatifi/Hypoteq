# Dependencies
/node_modules

# Build output
/.next
/out
/dist

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode
.idea

# Optional lock files (keep one if you want)
package-lock.json
yarn.lock
pnpm-lock.yaml

# TypeScript build info
*.tsbuildinfo

# Coverage reports
/coverage

# Logs
*.log

# GitHub Codespaces / VSCode specific
.history/

/generated/prisma
.vercel
.env*.local

# Generated test documents (scripts/writeTestPdfs.ts). The generator is the source, not
# the PDFs it produces — and a folder of documents in the repo invites someone to drop a
# real customer file next to them.
/test-documents
